[build]
builder = "nixpacks"

[deploy]
startCommand = "python run.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
healthcheckPath = "/healthz"
healthcheckTimeout = 30

# deploy: auto-deploy verification 22:28:29
