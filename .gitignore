# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Secrets & local config (never commit)
.env

# Runtime state (ephemeral — managed by Postgres on Railway)
signals.log

# Manual kill switch (presence halts new entries)
KILL_SWITCH

# Runtime logs + pids (ephemeral; bot writes these locally)
bot.out
bot.err
dash.out
dash.err
topstep-stdout.log
topstep-stderr.log
.pids
.DS_Store

# Runtime risk state (bot-written; do not track)
topstep_day_state.json
day_adapt.json
overnight-drift.log

# Research data (large; refetch via oos/fetch_databento.py)
oos/data/
oos/__pycache__/
exec_telemetry.jsonl
